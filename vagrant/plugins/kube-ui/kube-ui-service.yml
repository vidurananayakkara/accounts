---
apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: kube-ui
    kubernetes.io/cluster-service: "true"
    kubernetes.io/name: KubeUI
  name: kube-ui
  namespace: kube-system
spec:
  ports:
    -
      port: 80
      targetPort: 8080
  selector:
    k8s-app: kube-ui
