#!/usr/bin/env bash

# Flag to check the availability of the prerequisites
export PRE_REQ=1
export SCRIPT_PATH="$(pwd)"
export HOME_PATH="$(cd ../; pwd)"
export DOCKER_PATH="$HOME_PATH/deployment/docker"